{
  "name": "citizen-one",
  "private": true,
  "scripts": {
    "test": "npm --prefix backend test",
    "test:backend": "npm --prefix backend test",
    "i18n:merge-billing-profile": "node frontend/scripts/merge-billing-profile-locales.mjs",
    "e2e": "playwright test",
    "e2e:ui": "playwright test --ui",
    "e2e:install": "playwright install chromium",
    "depcheck": "npm --prefix frontend run depcheck && npm --prefix backend run depcheck",
    "depcheck:frontend": "npm --prefix frontend run depcheck",
    "depcheck:backend": "npm --prefix backend run depcheck",
    "audit:all": "npm audit --audit-level=high && npm --prefix frontend audit --audit-level=high && npm --prefix backend audit --audit-level=high",
    "audit:frontend": "npm --prefix frontend audit --audit-level=high",
    "audit:backend": "npm --prefix backend audit --audit-level=high",
    "audit:fix": "npm audit fix && npm --prefix frontend audit fix && npm --prefix backend audit fix"
  },
  "devDependencies": {
    "@playwright/test": "^1.51.0"
  }
}
