# Leave empty during `npm run dev` to use the Vite proxy (same-origin /api → backend).
# For production builds, set to your API origin, e.g. https://api.example.com
VITE_API_URL=

# Optional: backend URL for the dev proxy (default http://localhost:5000)
# VITE_PROXY_TARGET=http://localhost:5000
